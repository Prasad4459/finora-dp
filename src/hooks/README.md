# Hooks

React hooks that bridge UI to data. Backend hooks will wrap repositories
(typically via React Query) and expose ergonomic APIs to pages/components.

Existing local hooks (e.g. `use-mobile`) remain here. Backend hooks land
once tables and auth flows are enabled.